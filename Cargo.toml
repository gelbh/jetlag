[workspace]
members = ["crates/jetlag-geometry-kernel"]
resolver = "2"

# Kernel WASM release builds: LTO keeps geometry entrypoints competitive with TS.
[profile.release]
lto = true
codegen-units = 1
opt-level = 3
