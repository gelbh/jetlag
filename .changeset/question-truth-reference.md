---
"jetlag": patch
---

Question answers: clarify that recommended truth uses each hider's hiding-zone center until end game, then a frozen end-game GPS snapshot. Hider map shows an answer-reference pin at that point. End-game accept is blocked when any confirmed hider lacks a recent location.
