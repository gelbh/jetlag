---
"jetlag": patch
---

Host can reset session progress while keeping the same code. Home entry screen no longer scrolls under the top accent bar. Premium buttons stay stable across navigation. Leaving a session clears stale map tool state. Low power mode keeps sheet and panel motion visible.
