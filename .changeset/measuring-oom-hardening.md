---
"jetlag": patch
---

Measuring: large multi-place questions (e.g. many parks) use a lighter disk union so phones are less likely to run out of memory
Measuring: over-dense place lists or border data are blocked with a clear message instead of freezing the map
Session: if an answered question cannot be drawn, it cancels instead of crashing the map on every reload
