---
"jetlag": patch
---

Tentacle: answering a dense place list (e.g. many hospitals) no longer crashes the seeker map; failed draws cancel instead of retrying forever
Tentacle: very large place lists are refused before heavy shading work so phones stay responsive
