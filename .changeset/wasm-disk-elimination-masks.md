---
"jetlag": patch
---

End-game disk elimination masks use the WASM geometry kernel when available (override with `jl.geometry.maskKernel=ts`).

Technical: Disk WASM measured ≤1.0× TS on overlapping and ten-disk fixtures with topology parity (2026-07-29).
