name: Deploy

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy-backend:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    outputs:
      functions_deployed: ${{ steps.deploy.outputs.functions_deployed }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0
      - name: Detect backend changes
        id: changes
        run: |
          before="${{ github.event.workflow_run.head_sha }}^"
          if ! git rev-parse --verify "$before" >/dev/null 2>&1; then
            echo "backend=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          changed="$(git diff --name-only "$before" "${{ github.event.workflow_run.head_sha }}")"
          if echo "$changed" | grep -qE '^(functions/|firestore\.(rules|indexes\.json)|storage\.rules|firebase\.json|scripts/deploy-production\.mjs|\.firebaserc|src/services/billing/|src/routes/Premium\.tsx|src/services/admin/|src/hooks/admin/|src/routes/AdminPanel\.tsx)'; then
            echo "backend=true" >> "$GITHUB_OUTPUT"
          else
            echo "backend=false" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/setup-node@v4
        if: steps.changes.outputs.backend == 'true'
        with:
          node-version: 24
          cache: npm
      - run: npm ci
        if: steps.changes.outputs.backend == 'true'
      - name: Install Doppler CLI
        if: steps.changes.outputs.backend == 'true'
        uses: DopplerHQ/cli-action@v3
        with:
          doppler-token: ${{ secrets.DOPPLER_TOKEN }}
          verify-doppler-config: false
      - name: Load Doppler secrets
        if: steps.changes.outputs.backend == 'true'
        run: doppler secrets download --no-file --format docker >> "$GITHUB_ENV"
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}
      - uses: google-github-actions/auth@v2
        if: steps.changes.outputs.backend == 'true'
        with:
          workload_identity_provider: ${{ env.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.GCP_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2
        if: steps.changes.outputs.backend == 'true'
      - name: Deploy Firestore and Functions
        id: deploy
        if: steps.changes.outputs.backend == 'true'
        run: doppler run --config prd -- npm run deploy
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}

  deploy-frontend:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    needs: [deploy-backend]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0
      - name: Detect frontend changes
        id: changes
        run: |
          before="${{ github.event.workflow_run.head_sha }}^"
          if ! git rev-parse --verify "$before" >/dev/null 2>&1; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [[ "${{ needs.deploy-backend.outputs.functions_deployed }}" == "true" ]]; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          changed="$(git diff --name-only "$before" "${{ github.event.workflow_run.head_sha }}")"
          if echo "$changed" | grep -qE '^(src/|public/|worker/|index\.html|vite\.config\.ts|wrangler\.toml|package(-lock)?\.json|tsconfig.*\.json|tailwind\.css|eslint\.config\.js)'; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"
          else
            echo "frontend=false" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/setup-node@v4
        if: steps.changes.outputs.frontend == 'true'
        with:
          node-version: 24
          cache: npm
      - run: npm ci
        if: steps.changes.outputs.frontend == 'true'
      - name: Install Doppler CLI
        if: steps.changes.outputs.frontend == 'true'
        uses: DopplerHQ/cli-action@v3
        with:
          doppler-token: ${{ secrets.DOPPLER_TOKEN }}
          verify-doppler-config: false
      - name: Load Doppler secrets
        if: steps.changes.outputs.frontend == 'true'
        run: doppler secrets download --no-file --format docker >> "$GITHUB_ENV"
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}
      - name: Set CI-only env
        if: steps.changes.outputs.frontend == 'true'
        run: echo "VITE_SENTRY_RELEASE_DIST=${{ github.event.workflow_run.head_sha }}" >> "$GITHUB_ENV"
      - name: Require functions deploy when callables changed
        if: steps.changes.outputs.frontend == 'true'
        run: |
          before="${{ github.event.workflow_run.head_sha }}^"
          if ! git rev-parse --verify "$before" >/dev/null 2>&1; then
            exit 0
          fi
          changed="$(git diff --name-only "$before" "${{ github.event.workflow_run.head_sha }}")"
          if echo "$changed" | grep -qE '^functions/'; then
            if [[ "${{ needs.deploy-backend.outputs.functions_deployed }}" != "true" ]]; then
              echo "::error::Push includes functions/ changes but Cloud Functions were not deployed. Deploy backend first."
              exit 1
            fi
          fi
      - name: Require App Check site key
        if: steps.changes.outputs.frontend == 'true'
        run: |
          if [ -z "${VITE_FIREBASE_APP_CHECK_SITE_KEY}" ]; then
            echo "::error::VITE_FIREBASE_APP_CHECK_SITE_KEY is required for production builds."
            exit 1
          fi
      - name: Build production bundle
        if: steps.changes.outputs.frontend == 'true'
        run: doppler run --config prd -- npm run build 2>&1 | tee build.log
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}
      - name: Verify source maps are not shipped
        if: steps.changes.outputs.frontend == 'true'
        run: |
          if find dist -name '*.map' -print -quit | grep -q .; then
            echo "::error::Source map files remain in dist after build."
            exit 1
          fi
          if [ -f dist/_redirects ]; then
            echo "::error::dist/_redirects conflicts with wrangler.toml SPA handling. Remove public/_redirects."
            exit 1
          fi
          if ! grep -qiE 'uploaded|source maps' build.log; then
            echo "::warning::Sentry source map upload was not confirmed in build output."
          fi
      - uses: cloudflare/wrangler-action@v3
        if: steps.changes.outputs.frontend == 'true'
        with:
          apiToken: ${{ env.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ env.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
