/**
 * When false, production `MapView` always uses Leaflet even if `mapStore.mapEngine`
 * is `"maplibre"`. Flip true only after overlay/chrome ports can host play-map children.
 */
export const MAP_LIBRE_PLAY_READY = false;
