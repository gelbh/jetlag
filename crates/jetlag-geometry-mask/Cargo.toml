[package]
name = "jetlag-geometry-mask"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
geo = { version = "0.33.1", features = ["use-serde"] }
geojson = "0.24.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wasm-bindgen = "0.2"

[dev-dependencies]
